{
  "name": "incinerator-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter vechain build && pnpm --filter web build",
    "test": "pnpm -r test"
  }
}
